# Nancalacc Optimization Build Configuration
# 构建配置文件

# 项目基本信息
PROJECT_NAME="nancalacc"
BINARY_NAME="nancalacc"
VERSION="1.0.0"

# 构建目录
BUILD_DIR="bin"

# Docker 配置
DOCKER_REGISTRY="your-registry.com"
DOCKER_IMAGE="${DOCKER_REGISTRY}/${PROJECT_NAME}:${VERSION}"

# 默认环境
DEFAULT_ENV="dev"

# 平台配置
# 支持的平台: linux-amd64, linux-arm64, darwin-amd64, darwin-arm64
DEFAULT_PLATFORM="linux-amd64"

# 构建参数
BUILD_FLAGS="-X main.Version=${VERSION} -X main.Environment=${DEFAULT_ENV} -X main.BuildTime=$(date -u '+%Y-%m-%d_%H:%M:%S')"

# 环境特定配置
ENV_CONFIGS=(
    "dev:local:$(go env GOOS)-$(go env GOARCH)"
    "test:linux:linux-amd64"
    "prod:linux:linux-amd64"
) 